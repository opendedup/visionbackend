apiVersion: v1
kind: Service
metadata:
  name: {{ include "flexiblechart.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "flexiblechart.name" . }}
    helm.sh/chart: {{ include "flexiblechart.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "flexiblechart.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
