apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flexiblechart.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "flexiblechart.name" . }}
    helm.sh/chart: {{ include "flexiblechart.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "flexiblechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
